import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mspr-dev-angular',
  webDir: 'dist/mspr-dev-angular',
  bundledWebRuntime: false
};

export default config;
